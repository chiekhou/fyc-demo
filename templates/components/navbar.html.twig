<nav class="border-b border-gray-300 bg-white">
    <div class="mx-auto flex w-11/12 max-w-[1400px] items-center justify-between py-4">
        <a href="{{ path('app_home') }}" class="text-xl font-bold">
            <span class="text-blue-600">Demo</span>Fyc.
        </a>
        <div class="flex items-center">
            {% if app.user %}
                <a href="{{ path('app_dashboard') }}"
                    class="mr-8 flex items-center gap-2 text-sm text-blue-600">
                    <svg xmlns="http://www.w3.org/2000/svg"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke-width="1.5"
                        stroke="currentColor"
                        class="h-5 w-5">
                        <path stroke-linecap="round"
                            stroke-linejoin="round"
                            d="M2.25 12l8.954-8.955c.44-.439 1.152-.439 1.591 0L21.75 12M4.5 9.75v10.125c0 .621.504 1.125 1.125 1.125H9.75v-4.875c0-.621.504-1.125 1.125-1.125h2.25c.621 0 1.125.504 1.125 1.125V21h4.125c.621 0 1.125-.504 1.125-1.125V9.75M8.25 21h8.25" />
                    </svg>
                    <span class="hover:underline">Dashboard</span>
                </a>

                 <a href="{{ path('app_logout') }}"
                    class="inline-flex justify-center rounded-md border border-transparent bg-blue-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    Logout
                </a>
          
                <div class="px-3 navbar-user flex items-center gap-3">
            
                    <span class="flex items-center gap-2">
                        <span class="text-sm font-medium">
                            {{ app.user.firstname }} {{ app.user.lastname }}
                        </span>
                    </span>
                </div>

               
            {% else %}
                <a href="{{ path('app_login') }}"
                    class="mr-2 inline-flex justify-center rounded-md border border-blue-600 bg-transparent py-2 px-4 text-sm font-medium text-blue-600 shadow-sm hover:border-indigo-700 hover:text-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'identifier
                </a>
                <a href="{{ path('app_register') }}"
                    class="inline-flex justify-center rounded-md border border-transparent bg-blue-600 py-2 px-4 text-sm font-medium text-white shadow-sm hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">
                    S'inscrire
                </a>
            {% endif %}
        </div>
    </div>
</nav>